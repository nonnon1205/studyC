{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch threadText (Makefile build)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/threadText",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "Makefileを実行",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
